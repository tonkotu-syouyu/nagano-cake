<h2>マイページ</h2>

<h3>登録情報</h3>
<%= link_to "編集する", edit_public_customer_path(@customer.id), class: 'btn btn-success' %>

<table class="table">
  <tbody>
    <tr>
      <th>氏名</t>
      <th class="customer-name"><%= @customer.last_name %></th>
      <th class="customer-name"><%= @customer.first_name %></th>
    </tr>
    <tr>
      <th>カナ</th>
      <th class="customer-name-kana"><%= @customer.last_name_kana %></th>
      <th class="customer-name"><%= @customer.first_name_kana %></th>
    </tr>
    <tr>
      <th>郵便番号</th>
      <th class="customer-postal-code"><%= @customer.postal_code %></th>
    </tr>
    <tr>
      <th>住所</th>
      <th class="customer-address"><%= @customer.address %></th>
    </tr>
    <tr>
      <th>電話番号</th>
      <th class="customer-telephone-number"><%= @customer.telephone_number %></th>
    </tr>
    <tr>
      <th>メールアドレス</th>
      <th class="customer-email"><%= @customer.email %></th>
    </tr>
  </tbody>
</table>

<h2>配送先</h2>
<%= link_to "一覧を見る", public_addresses_path(@customer.id), class: "btn btn-primary" %>

<h2>注文履歴</h2>
<%= link_to "一覧を見る", public_order_path(@customer.id), class: "btn btn-primary" %>