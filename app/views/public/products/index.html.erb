<div class="container">
  <div class="row">
    <div class="mx-auto">
      <title class="pull-left">
        <h2>商品一覧</h2><p>全x件</p>
      </title>
      <%= @products.each do |product| %>
      <%= image_tag product.image_id %>
      <%= product.name %>
      <%= product.price %>
      <% end %>
    </div>
  </div>
</div>


<%= paginate @products %>