<div class="container">
  <div class="row">
    <div class="col-lg-4">
      <h4>注文情報入力</h4>
      <div class="field">
        <%= form_with model: @order, url: public_orders_confirm_path, local: true do |f| %>
          <span class="payment_method">支払い情報</span><br>
          <div class="radio-inline">
            <%= f.radio_button :payment_method, :クレジットカード, checked: "checked" %>
            <%= f.label :クレジットカード, class: "payment_method_label" %>
          </div>
          <div class="radio-inline">
            <%= f.radio_button :payment_method, :銀行振込 %>
            <%= f.label :銀光振込, class: "payment_method_label" %>
          </div>
          <span class="address">お届け先</span><br>
          <div class="radio-inline">
            <%= f.radio_button :address_option, 0, checked: "checked" %>
            <%= f.label :order_address, "ご自身の住所" %>
            <%= @customer.postal_code %>
            <%= @customer.address %>
          </div>
          <div class="radio-inline">
            <%= f.radio_button :address_option, 1 %>
            <%= f.label :order_address, "登録済住所から選択" %>
            <%# f.collection_select :order_address , current_customer.addresses, :id, :address , :include_blank => "選択してください" %>
          </div>
          <div class="radio-inline">
            <%= f.radio_button :address_option, 2 %>
            <%= f.label :order_address, "新しいお届け先" %>
          </div>
            <%= f.label :postal_code, "郵便番号" %>
            <%= f.text_field :postal_code %>
            <br>
            <%= f.label :order_address, "住所" %>
            <%= f.text_field :order_address %>
            <br>
            <%= f.label :addresses_name, "宛名" %>
            <%= f.text_field :addresses_name %>
            <%= f.submit "確認画面へ進む" , class:"btn btn-primary" %>
        <% end %>

      </div>
    </div>
  </div>

</div>

