<div class="container">
  <div class="row">
    <div class="col-xs-10 col-xs-offset-1">
      <h2 class="head-line title_h2 text-center">
      <%= @customer.first_name %>
      <%= @customer.last_name %>さんの会員詳細
      </h2>
        <table class="row table admin-customers-show-table">
        <tr>
           <th class="col-xs-2">顧客ID</th>
            <td class="col-xs-10">
              <%= @customer.id %></td>
        </tr>

        <tr>
          <th>氏名</th>
          <td><%= @customer.first_name %>
              <%= @customer.last_name %></td>
        </tr>

        <tr>
          <th>フリガナ</th>
          <td><%= @customer.first_name_kana %>
              <%= @customer.last_name_kana %></td>
        </tr>

        <tr>
          <th>郵便番号</th>
          <td><%= @customer.postal_code %></td>
        </tr>

        <tr>
          <th>住所</th>
          <td><%= @customer.address %></td>
        </tr>

        <tr>
          <th>電話番号</th>
          <td><%= @customer.telephone_number %></td>
        </tr>

        <tr>
          <th>メールアドレス</th>
          <td><%= @customer.email %></td>
        </tr>

        <tr>
          <th>会員ステータス</th>
          <td>
            <% if @customer.is_deleted %>
              <span class="label label-primary is_deleted_label">退会</span>
            <% else %>
              <span class="label label-danger is_deleted_label">有効</span>
            <% end %>
          </td>
        </tr>
        </table>
        <div class="row">
          <%= link_to "編集する", edit_admin_customer_path(@customer), class: "btn btn-success" %>
          <%= link_to " 注文履歴一覧を見る", admin_root_path(@customer), class: "glyphicon glyphicon-folder-open btn btn-info" %>
        </div>
      </div>
    </div>
  </div>